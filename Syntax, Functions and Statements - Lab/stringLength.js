function stringLength(a, b, c) {
    let lenSum = a.length + b.length + c.length
    let avg = Math.floor(lenSum/3)
    console.log(lenSum);
    console.log(avg);
}
stringLength('pasta', '5', '22.3')